version: 0.2
phases:
  build:
    commands:
      - echo "Build started"
artifacts:
  files:
    - '**/*'

batch:
  fast-fail: false
  build-list:
    - identifier: build1
      buildspec: buildspec.yml
